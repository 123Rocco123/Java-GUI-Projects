class main {
  public static void main(String[] args) {
    // Since we only need to start the game with the calling of the GameFrame class, we don't have to save it to a variable.
       // Instead, we can just create a new instance of the class as seen below. 
    new GameFrame();
  }
}
